import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Regex Generator | Zyntax",
  description: "Visual regex builder with pattern library and testing capabilities",
};
